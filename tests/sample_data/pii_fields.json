{
    "configurations": [
        {
            "name": "basic_pii",
            "fields": ["name", "email_address"],
            "description": "Basic PII fields that should always be obfuscated"
        },
        {
            "name": "extended_pii",
            "fields": ["name", "email_address", "phone_number"],
            "description": "Extended PII fields including contact information"
        },
        {
            "name": "minimal_pii",
            "fields": ["name"],
            "description": "Minimal PII fields for basic anonymization"
        },
        {
            "name": "invalid_fields",
            "fields": ["name", "non_existent_field"],
            "description": "Configuration with invalid field for testing error handling"
        }
    ]
}